[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trajektia-database"
version = "0.1.0"
description = "Trajektia Database schemas & migrations"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
